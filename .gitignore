# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
build/
dist/

# Runtime data: uploads, local logs, tunnel logs, screenshots taken during checks.
# The curated showcase images under docs/screenshots are committed on purpose.
.data/

# Environment. A real .env holds the database URL and the session secret.
.env
.env.local
.env*.local

# Prisma generated client. Regenerated by `npm run db:generate`, and its size
# makes it a poor fit for a repository.
src/generated/

# Test and tooling output
coverage/
*.tsbuildinfo
next-env.d.ts

# Editor and OS noise
.DS_Store
Thumbs.db
.idea/
*.swp

# Local overrides
.vercel
